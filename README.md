// Instrucciones para correr el proyecto:

// 1. Ejecutar el comando 'npm install' en consola

// 2. Crear un archivo '.env' en la raíz del proyecto con las siguientes variables
PORT=3000
DB_USER=RamaFurtado
DB_PASSWORD=yamiyami2020

// 3. Ejecutar el comando 'npm run dev' en consola
